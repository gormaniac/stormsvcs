name: Report Ingest Pipeline

on:
  workflow_call:
  pull_request:
    paths:
      - "svcs/report-ingest/**"

jobs:

  docker:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build Docker image
      run: |
        echo "Building Docker image..."
        # Add your Docker build commands here

  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Run tests
      run: |
        echo "Running tests..."
        # Add your test commands here

  publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Publish
      run: |
        echo "Publishing..."
        # Add your publish commands here

